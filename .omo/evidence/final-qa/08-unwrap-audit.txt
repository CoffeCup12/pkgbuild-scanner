TEST 8: Check no .unwrap() in non-test code
Method: Examined every .unwrap() call across all src/*.rs files.
Checked whether each appears within a #[cfg(test)] module.

Result: PASS — ZERO unwraps in production code.

All 64 .unwrap() calls reside exclusively in #[cfg(test)] mod tests blocks:
  src/aur.rs:      17 calls (all test)
  src/cache.rs:     4 calls (all test)
  src/config.rs:    5 calls (all test)
  src/extract.rs:  14 calls (all test)
  src/ollama.rs:    1 call  (all test)
  src/scanner.rs:  14 calls (all test)
  src/types.rs:     9 calls (all test)
  main, exec, routes, interactive, prompt, version: 0 calls

Production code uses proper error handling (Result propagation, Option combinators,
unwrap_or, etc.) throughout.
